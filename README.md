# NHL Goal Lights HACS Release

## Installation

1. Copy all files into a folder `nhl_goal_lights_hacs_release`
2. Zip the folder:

```bash
zip -r nhl_goal_lights_hacs_release.zip nhl_goal_lights_hacs_release/